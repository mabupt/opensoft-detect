# 安全策略

## 报告安全问题

若你在本项目自身发现安全漏洞（例如沙箱逃逸、命令注入、凭据处理缺陷），请**不要**公开提交 Issue，
改为通过 GitHub 的 [Private vulnerability reporting](https://github.com/mabupt/opensoft-detect/security/advisories/new)
或邮件私下告知维护者，并在报告中包含：

- 受影响版本/提交；
- 复现步骤或最小复现样例；
- 影响评估（可否导致被测工程或宿主受损）。

维护者会在确认后尽快修复并在 Release Notes 中致谢（如你希望署名）。

## 支持范围

项目处于早期版本（0.x），仅对 `main` 分支的最新提交提供安全修复。

## 使用须知（重要）

本项目是**漏洞检测工具**，其设计目标包含"在隔离沙箱内真实触发被测代码的漏洞"：

1. **仅在你有权测试的目标上使用**。未经授权对他人系统进行扫描或验证可能违法。
2. **动态验证会产生真实副作用**。探针会真实执行被测代码中的危险操作（命令执行、反序列化、
   文件写入等）。系统已做隔离：被测工程以**副本**形式挂载进容器（仓库外目标挂载于 `/target`），
   运行后统一回收容器——但仍建议在专用测试环境中运行。
3. **Docker 隔离不是绝对安全边界**。容器逃逸类漏洞客观存在，请勿对来源不明的代码
   在生产宿主机上开启动态验证。
4. **LLM 调用会将代码片段发送至你配置的模型服务**。若被测代码涉密，请使用私有化部署的模型端点，
   或通过 `--skip-llm` 关闭研判阶段。
5. **检测结果不构成安全保证**。工具存在已知边界（见 [docs/HARDENING.md](docs/HARDENING.md) §四），
   未检出 ≠ 无漏洞，报告结论需结合人工复核。
